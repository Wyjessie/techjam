# Leave both unset to use the built-in mock predictor (no model needed).
# Once the model is deployed somewhere in the cloud, point these at it
# and the backend will start calling it for real — no code changes needed.
MODEL_API_URL=
MODEL_API_KEY=
